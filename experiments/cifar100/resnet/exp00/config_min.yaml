device: cuda:1
dataset:
  name: CIFAR100
  dataset_dir: ~/.torch/datasets/CIFAR100
  n_classes: 100
model:
  name: resnet
train:
  output_dir: experiments/cifar100/resnet/exp00
  checkpoint_period: 100